spring:
  application:
    name: "MockProject_052024_Group1"

  datasource:
    url: jdbc:sqlserver://localhost:1433; trustServerCertificate=true; databaseName=mock_project
    username: sa
    password: MockProject_052024
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect

security:
  jwt:
    token:
      secret-key: ${JWT_SECRET}
      accessTokenValidityMs: 1800000 # 30 minutes
